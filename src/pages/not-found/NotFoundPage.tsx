import { Typography } from '@mui/material';

// Todo: add an icon here:

const NotFoundPage = () => {
  return (
    <div>
      <Typography className="text-center mt-20" variant="h5" color="textMuted">
        Page Not Found
      </Typography>
    </div>
  );
};
export default NotFoundPage;
